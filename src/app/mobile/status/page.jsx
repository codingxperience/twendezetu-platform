import { MobileStatusAssets } from '../MobileShowcase';

export const metadata = {
  title: 'Twendezetu Mobile Status Assets',
  robots: {
    index: false,
    follow: false,
  },
};

export default function MobileStatusAssetsPage() {
  return <MobileStatusAssets />;
}
